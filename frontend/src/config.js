const config = {
  API_URL: process.env.REACT_APP_API_URL || 'https://stock-broker-website.onrender.com',
  WS_URL: process.env.REACT_APP_WS_URL || 'https://stock-broker-website.onrender.com'
};

export default config;
